[return](gnuhealth)

:python:

```sh

#rume or sudo
sudo apt install python3 python3-pip
python -m pip install trytond


#if python is already installed, and you want to install pip3 only,
#write this command

#rume or sudo
sudo apt-get -y install python3-pip

#to uninstall pip3 only
sudo pip3 uninstall pip

```




